[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
startCommand = "node migrations/run.js && echo 'MIGRATIONS DONE' && echo 'STARTING SERVER' && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
